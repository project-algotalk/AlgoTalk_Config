server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/project-algotalk/AlgoTalk_Config
          default-label: main
          search-paths: msa